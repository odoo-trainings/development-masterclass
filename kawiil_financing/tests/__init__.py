# TODO (3.07): add the import for your test module here:
#     from . import test_loan_application
#
# Odoo finds the tests/ package on its own — it must not be imported from the
# module's main __init__.py, or the test code would load in every database that
# installs this module, not only the ones running tests. This file is the one place
# the test modules get listed, and a test module missing from it simply never runs:
# the suite reports success having executed nothing.
